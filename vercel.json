{
  "version": 2,
  "buildCommand": "pnpm install --ignore-scripts --frozen-lockfile && pnpm run vercel-build",
  "installCommand": "pnpm install --ignore-scripts --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "functions": {
    "src/app/api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
