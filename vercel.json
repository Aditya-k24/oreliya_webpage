{
  "buildCommand": "cd apps/web-next && pnpm install --ignore-scripts && pnpm add typescript --save-dev --ignore-scripts && pnpm run build",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
